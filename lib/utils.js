class Utils {

    static encrypt(data, encryption_key) {
        //Should be encrypted in the future
        return data;
    }

    static decrypt(hash, encryption_key) {
        //Should be decryped here
        return hash;
    }
}

module.exports = Utils;